import React from 'react';

const SomeText = () => (
    <div>
        <p>To horizontally center a block element, 
            use margin: auto;Setting the width of the element will prevent it from 
            stretching out to the edges of its container.
            The element will then take up the specified width, and the 
            remaining space will be split equally between the two margins:
        </p>
    </div>
);
export default SomeText;
